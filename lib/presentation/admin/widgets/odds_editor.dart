import 'package:flutter/material.dart';

class OddsEditor extends StatelessWidget {
  const OddsEditor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card();
  }
}
