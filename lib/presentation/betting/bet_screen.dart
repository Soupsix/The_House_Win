import 'package:flutter/material.dart';

class BetScreen extends StatelessWidget {
  const BetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Bet Screen')),
    );
  }
}
