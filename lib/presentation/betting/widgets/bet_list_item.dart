import 'package:flutter/material.dart';

class BetListItem extends StatelessWidget {
  const BetListItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile();
  }
}
