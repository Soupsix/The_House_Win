import 'package:flutter/material.dart';

class BetConfirmSheet extends StatelessWidget {
  const BetConfirmSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 300);
  }
}
