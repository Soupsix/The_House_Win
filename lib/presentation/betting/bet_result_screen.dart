import 'package:flutter/material.dart';

class BetResultScreen extends StatelessWidget {
  const BetResultScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Bet Result Screen')),
    );
  }
}
