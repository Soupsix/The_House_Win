import 'package:flutter/material.dart';

class OddsInputCard extends StatelessWidget {
  const OddsInputCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card();
  }
}
