import 'package:flutter/material.dart';

class FallacyScreen extends StatelessWidget {
  const FallacyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Fallacy Screen')),
    );
  }
}
