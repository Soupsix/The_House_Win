import 'package:flutter/material.dart';

class OddsDisplay extends StatelessWidget {
  const OddsDisplay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(
      children: [
        Text('Odds: 1.5'),
      ],
    );
  }
}
