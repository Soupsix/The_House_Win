import 'package:flutter/material.dart';

class LiveScoreBadge extends StatelessWidget {
  const LiveScoreBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Chip(label: Text('LIVE'));
  }
}
